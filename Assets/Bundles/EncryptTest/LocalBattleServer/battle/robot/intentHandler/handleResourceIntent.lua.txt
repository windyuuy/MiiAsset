function Execute(robot, intent)
    local battleData = robot.battleData
    local old = battleData.mana
    battleData.mana = intent.resource.mana
    --LogDebug("handlerResourceIntent.Execute old:{} new:{}", old, battleData.mana)
end