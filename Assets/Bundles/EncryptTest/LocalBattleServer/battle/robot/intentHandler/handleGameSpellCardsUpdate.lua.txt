function Execute(robot, intent)
    local battleData = robot.battleData
    if robot.id ~= intent.gameSpellCardsUpdate.playerId then
        return
    end
    battleData.spellCards = intent.gameSpellCardsUpdate.spellCards
end